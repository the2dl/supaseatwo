# WMI Command Execution Utility

This module provides a function to execute commands on remote machines via Windows Management Instrumentation (WMI).

## Functions:
* `wmirun`: Executes a command on a remote machine via WMI.